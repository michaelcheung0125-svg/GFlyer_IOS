# Xcode and local build output
.build/
DerivedData/
*.xcodeproj/
*.xcworkspace/
*.xcarchive/
*.ipa
*.dSYM/
xcuserdata/
*.xcuserstate

# Locally built idevice bridge
GFlyerIOS/Vendor/idevice/
libidevice_ffi.a

# Apple signing material and device credentials
*.mobiledevicepairing
*.mobileprovision
*.p12
*.pfx
signing/
secrets/

# Downloaded developer images
*.dmg
PersonalizedImages/

# Local environment files
.DS_Store
.env
.env.*
!.env.example

# 74MB 的角色圖素材：本機可用，但不進 repo（CI 每次 push 都會 checkout）。
# 要在其他機器使用，從 https://github.com/Unclecheng-li/AI_Animation 重新複製。
.claude/skills/video-shot-demos/
